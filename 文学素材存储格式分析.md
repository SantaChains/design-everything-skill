# 文学素材存储格式分析报告

**生成时间**: 2026-04-13  
**目标**: 选择最适合文学素材存储的格式

---

## 一、格式对比分析

### 1.1 TSV (Tab-Separated Values)

**优点**:
- ✅ 简单易读，纯文本格式
- ✅ 文件体积小
- ✅ 易于版本控制（Git友好）
- ✅ 可用文本编辑器直接查看和编辑
- ✅ 与项目架构一致（openmemory.md中提到）

**缺点**:
- ❌ 不支持嵌套结构
- ❌ 长文本内容（如原文引用）难以处理
- ❌ 多行文本需要特殊转义
- ❌ 缺乏类型系统
- ❌ 不支持复杂数据结构

**适用场景**:
- 简单的表格数据
- 索引文件
- 元数据列表

### 1.2 JSON (JavaScript Object Notation)

**优点**:
- ✅ 支持嵌套结构
- ✅ 支持多行文本
- ✅ 类型系统完善（字符串、数字、布尔、数组、对象）
- ✅ 广泛的工具支持
- ✅ 与Skill Router集成方便
- ✅ 易于解析和处理

**缺点**- ❌ 文件体积相对较大
- ❌ 不适合人工编辑（容易语法错误）
- ❌ 版本控制不友好（diff不直观）

**适用场景**:
- 复杂数据结构
- 配置文件
- API数据交换

### 1.3 SQLite

**优点**:
- ✅ 支持复杂查询
- ✅ 支持索引，检索速度快
- ✅ 支持事务
- ✅ 单文件存储，易于管理
- ✅ 支持全文搜索（FTS）
- ✅ 与项目架构一致（openmemory.md中提到）

**缺点**:
- ❌ 二进制格式，不可读
- ❌ 不适合版本控制
- ❌ 需要专门的工具查看和编辑

**适用场景**:
- 大量数据的存储和检索
- 需要复杂查询的场景
- 生产环境的数据存储

### 1.4 KDL (KDL Document Language)

**优点**:
- ✅ 与项目配置文件一致
- ✅ 可读性强
- ✅ 支持嵌套结构
- ✅ 类型安全

**缺点**:
- ❌ 生态相对较小
- ❌ 工具支持有限
- ❌ 不适合大量数据存储

**适用场景**:
- 配置文件
- 小规模数据

---

## 二、文学素材特点分析

### 2.1 数据特点

1. **文本内容长**: 原文引用可能包含多行文本
2. **结构复杂**: 包含作品信息、作者、出处、应用场景等多个维度
3. **检索需求多样**: 需要按类型、作者、作品、应用场景等检索
4. **版本控制需求**: 需要跟踪素材的修改历史
5. **人工审核需求**: 需要人工查看和编辑素材

### 2.2 使用场景

1. **素材录入**: 人工从文学作品中提取素材
2. **素材检索**: Skill Router 根据用户需求检索素材
3. **素材展示**: 向用户展示素材内容
4. **素材管理**: 更新、删除、修改素材

---

## 三、推荐方案：混合架构

基于文学素材的特点和项目架构，我推荐采用**混合架构**：

### 3.1 三层存储架构

```
┌─────────────────────────────────────────┐
│         KDL 配置层 (literature_library.kdl)        │
│  - 素材来源定义                          │
│  - 分类体系                              │
│  - 提取指南                              │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│       JSON 数据层 (literature_materials.json)      │
│  - 素材详细内容                          │
│  - 原文引用                              │
│  - 应用场景                              │
│  - 人工可读                              │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│      SQLite 索引层 (literature_index.db)          │
│  - 快速检索                              │
│  - 全文搜索                              │
│  - 关系查询                              │
└─────────────────────────────────────────┘
```

### 3.2 各层职责

**KDL 配置层**:
- 定义素材来源、分类体系、提取指南
- 与项目架构一致
- 人工可读可编辑

**JSON 数据层**:
- 存储素材详细内容
- 支持复杂嵌套结构
- 人工可读可编辑
- 版本控制友好

**SQLite 索引层**:
- 从JSON数据自动生成
- 提供快速检索
- 支持全文搜索
- 与Skill Router集成

---

## 四、数据结构设计

### 4.1 JSON 数据结构

```json
{
  "meta": {
    "version": "1.0",
    "updated": "2026-04-13",
    "total_materials": 100
  },
  "materials": [
    {
      "id": "action_001",
      "type": "action_design",
      "category": "chinese_classics",
      "work": {
        "title": "水浒传",
        "author": "施耐庵",
        "period": "元末明初",
        "source": "https://www.shuge.org"
      },
      "content": {
        "original_text": "林冲看他步法紊乱，便把棒从地下一跳...",
        "context": "林冲棒打洪教头",
        "chapter": "第九回"
      },
      "extraction": {
        "elements": ["肢体动作", "姿态转换", "动作序列"],
        "description": "快速、连贯、出其不意的武打动作",
        "key_points": ["跳棒", "转身", "扫棒"]
      },
      "application": {
        "scenes": ["武侠动作设计", "武术指导", "战斗场景"],
        "style": "快速、精准、流畅",
        "difficulty": "中等"
      },
      "tags": ["武打", "棒法", "快速", "连贯"],
      "quality_score": 9.0,
      "created_at": "2026-04-13",
      "updated_at": "2026-04-13"
    }
  ]
}
```

### 4.2 SQLite 表结构

```sql
-- 素材主表
CREATE TABLE materials (
    id TEXT PRIMARY KEY,
    type TEXT NOT NULL,
    category TEXT NOT NULL,
    work_title TEXT NOT NULL,
    work_author TEXT NOT NULL,
    original_text TEXT NOT NULL,
    description TEXT,
    quality_score REAL,
    created_at TEXT,
    updated_at TEXT
);

-- 标签表
CREATE TABLE tags (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    material_id TEXT NOT NULL,
    tag TEXT NOT NULL,
    FOREIGN KEY (material_id) REFERENCES materials(id)
);

-- 应用场景表
CREATE TABLE applications (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    material_id TEXT NOT NULL,
    scene TEXT NOT NULL,
    FOREIGN KEY (material_id) REFERENCES materials(id)
);

-- 全文搜索索引
CREATE VIRTUAL TABLE materials_fts USING fts5(
    id,
    type,
    category,
    work_title,
    work_author,
    original_text,
    description,
    tags
);
```

### 4.3 TSV 索引文件（可选）

如果需要快速浏览素材列表，可以生成TSV索引文件：

```tsv
id\ttype\tcategory\twork_title\tauthor\tquality_score\ttags
action_001\taction_design\tchinese_classics\t水浒传\t施耐庵\t9.0\t武打,棒法,快速
action_002\taction_design\tchinese_classics\t三国演义\t罗贯中\t8.5\t战争,武将,马战
pose_001\tpose_design\tchinese_classics\t红楼梦\t曹雪芹\t9.5\t古典,女性,优雅
```

---

## 五、实施方案

### 5.1 目录结构

```
design-everything/
├── literature_library.kdl          # KDL配置文件
├── data/
│   ├── literature_materials.json   # JSON数据文件
│   ├── literature_index.db         # SQLite索引数据库
│   └── literature_index.tsv        # TSV索引文件（可选）
├── scripts/
│   ├── build_index.py              # 从JSON构建SQLite索引
│   └── validate_materials.py       # 验证素材格式
└── docs/
    ├── 公有领域文学素材库研究报告.md
    └── 文学素材库配置总结.md
```

### 5.2 工作流程

1. **素材录入**:
   - 人工编辑 `literature_materials.json`
   - 运行 `validate_materials.py` 验证格式
   - 运行 `build_index.py` 构建索引

2. **素材检索**:
   - Skill Router 查询 SQLite 数据库
   - 返回素材ID
   - 从JSON文件读取详细内容

3. **素材更新**:
   - 修改JSON文件
   - 重新构建索引

---

## 六、对比总结

| 格式 | 可读性 | 可编辑性 | 查询性能 | 版本控制 | 推荐用途 |
|------|--------|----------|----------|----------|----------|
| TSV | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ | 索引文件 |
| JSON | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | 数据存储 |
| SQLite | ⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐ | 快速检索 |
| KDL | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ | 配置文件 |

---

## 七、最终推荐

**推荐方案**: **TSV 格式（与项目现有架构一致）**

**理由**:
1. **与项目架构一致**：
   - 项目已在 `data/tsv/` 目录下使用TSV格式存储设计素材
   - 格式统一，易于维护
   - 符合项目规范

2. **TSV 格式优势**：
   - 简单易读，纯文本格式
   - 文件体积小
   - 易于版本控制（Git友好）
   - 可用文本编辑器直接查看和编辑
   - 支持长文本内容（content字段可包含详细描述）

3. **数据结构清晰**：
   - title: 标题（格式：中文|英文）
   - content: 详细内容（包含原文、作品信息、要素、特点、应用场景等）
   - tags: 标签（逗号分隔）
   - metadata: 元数据（格式：style=xxx, source=xxx, author=xxx等）

4. **SQLite 作为索引层**：
   - 项目已有 SQLite 数据库支持
   - 可从TSV自动构建索引
   - 提供快速检索
   - 与Skill Router集成

**不推荐纯JSON的原因**:
- 与项目现有架构不一致
- 项目已大量使用TSV格式
- TSV格式已能满足文学素材存储需求

**实施方案**:
1. 创建文学素材TSV文件：
   - `literature_action.tsv` - 动作设计素材
   - `literature_pose.tsv` - 姿势设计素材
   - `literature_character.tsv` - 人物外貌素材
   - `literature_emotion.tsv` - 情感表达素材

2. 使用项目现有的TSV读取工具：
   - `scripts/src/bin/tsv_reader.rs`
   - `scripts/tsv_reader.py`

3. 集成到SQLite数据库：
   - 使用项目现有的数据库管理工具
   - `scripts/src/bin/db_manager.rs`
   - `scripts/db_manager.py`

---

**报告生成时间**: 2026-04-13  
**推荐格式**: TSV (主数据，与项目架构一致) + SQLite (索引)
