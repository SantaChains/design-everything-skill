
## 核心Skill最小集合

### 1. brainstorming — 创意前置必经

**核心价值**：任何创造性工作前必须执行的意图探索和设计验证流程。

**核心流程**：
```
理解上下文 → 逐个提问细化 → 探索2-3种方案 → 分段呈现设计 → 增量验证
```

**关键原则**：
- **一次一问** — 不压倒用户，多选优先
- **YAGNI无情** — 删除所有不必要功能
- **探索替代** — 永远提出2-3种方案再决定
- **增量验证** — 200-300字分段呈现，每段确认

**输出物**：`docs/plans/YYYY-MM-DD-<topic>-design.md` + 实施计划

**犀利洞察**：大多数项目失败不是因为技术，而是因为**需求理解偏差**。brainstorming强制在写代码前完成"想清楚"这个步骤。

---

### 2. self-improving — 智能体自我进化引擎

**核心价值**：让AI从错误中永久学习，知识随时间复合增长。

**核心架构**：
```
HOT(≤100行，常驻) → WARM(≤200行，按需) → COLD(归档，显式查询)
```

**自动晋升/降级机制**：
| 条件 | 动作 |
|------|------|
| 7天内使用3次 | 晋升HOT |
| 30天未用 | 降级WARM |
| 90天未用 | 归档COLD |

**命名空间隔离**：
- `projects/{name}.md` — 项目级偏好
- `domains/{code,writing}.md` — 领域级模式
- `memory.md` — 全局偏好(HOT)

**冲突解决**：项目级 > 领域级 > 全局级，同级别最新优先

**犀利洞察**：传统AI助手每次对话都从零开始，self-improving让AI**拥有记忆**，形成"越用越懂你"的正反馈循环。这是从"工具"到"伙伴"的关键跃迁。

---

### 3. active-research — 深度研究与分析引擎

**核心价值**：对任何主题进行多源深度研究，生成结构化HTML报告。

**核心技术栈**：
- **Actionbook Browser** — SPA感知导航、网络空闲检测、批量操作、反检测
- **arXiv高级搜索** — 40+字段筛选，学术论文首选
- **Google/Bing补充** — 博客、新闻、社区讨论
- **json-ui渲染** — 结构化报告生成

**关键能力矩阵**：
| 能力 | 实现 |
|------|------|
| SPA内容提取 | `wait-idle` + `wait-fn` |
| 反爬虫绕过 | `--stealth` + `fingerprint rotate` |
| 静态页面加速 | `--lite` 跳过浏览器 |
| URL反检测 | `--rewrite-urls` (x.com→xcancel.com) |
| 批量表单操作 | `batch` 命令 |
| 智能页面理解 | `snapshot --filter interactive` |

**研究流程**：
```
规划搜索策略 → Actionbook API查询选择器 → arXiv搜索 → Google/Bing补充 → 深度阅读 → 综合分析 → JSON报告 → HTML渲染
```

**犀利洞察**：传统搜索只是"找到链接"，active-research是**"理解并结构化"**。它不只是抓取网页，而是构建知识图谱，生成可复用的研究报告。

---

## 三者协同逻辑

```
brainstorming (需求澄清)
        ↓
active-research (技术调研)
        ↓
self-improving (经验沉淀)
```

**设计哲学**：
1. **brainstorming**确保"做正确的事" — 需求对齐
2. **active-research**确保"正确地做事" — 技术验证
3. **self-improving**确保"越做越好" — 经验复用

**最小集合原则**：这三个skill覆盖了AI辅助设计的**完整闭环** — 从想法到实现到进化。任何额外skill都是锦上添花，这三个是雪中送炭。

---

## ToolCall最佳实践

### brainstorming触发时机
- 用户说"我想做一个..."、"帮我设计..."、"添加一个功能..."
- **强制前置**：任何涉及架构决策、新功能开发、重大重构

### self-improving触发时机
- 用户纠正你："不对，应该是..."
- 完成重要任务后自省
- 发现自己输出可优化时
- **持续运行**：每次对话结束前检查是否需要记录

### active-research触发时机
- 用户说"研究..."、"分析..."、"调查..."、"生成报告..."
- 需要技术选型验证
- 需要了解最新进展
- **按需调用**：遇到知识盲区时主动触发

---

## 禁止事项

- **禁止**在brainstorming未完成前直接写代码
- **禁止**存储敏感信息(credentials, health data, third-party info)
- **禁止**使用curl/wget/python requests — 必须用Actionbook Browser
- **禁止**手动构造URL — 必须从snapshot中提取真实链接
- **禁止**重试404页面 — 立即跳过，使用snippet作为备份数据

---

## 验证标准

| Skill | 成功标准 |
|-------|---------|
| brainstorming | 设计文档经用户确认，无重大返工 |
| self-improving | 重复错误率下降，用户满意度提升 |
| active-research | 报告包含3-5个独立来源，关键信息交叉验证 |

---

## 设计规范限制

1. **可行性优先** — 不可行的方案不提出
2. **资料正确性** — 所有引用必须可验证
3. **最佳实践导向** — 优先推荐行业标准方案
4. **最强实现目标** — 在可行性范围内追求最优解

**犀利总结**：这三个skill不是"功能列表"，而是**"思维框架"**。它们教会AI如何思考、如何学习、如何研究。掌握了这三个，就掌握了AI辅助设计的核心方法论。
