优先级排序 :

1. P0 (立即实施) : Skill Router 系统 - 这是核心创新点
2. P1 (1周内) : KDL 配置系统 - 提升架构灵活性
3. P2 (2周内) : 文学素材库 - 提升输出质量
4. P3 (持续) : TSV 内容增强 - 长期优化
技术栈建议 :

- KDL 解析 : 使用 kdl-py 库
- 模糊度计算 : 基于关键词密度和上下文完整性
- 文学素材 : 使用 NLP 技术自动提取(可选)
风险提示 :

- KDL 是较新的配置语言,生态不如 YAML/JSON 成熟
- Skill Router 需要大量测试确保不会误触发
- 文学素材涉及版权问题,建议使用公有领域作品或自行创作



### 短期优化（1-2 周）
1. 路径处理优化 ：实现更智能的跨平台路径检测
2. 错误提示优化 ：添加更友好的错误信息
3. 日志系统 ：添加调试日志便于排查问题
### 中期扩展（1-2 月）
1. KDL 配置支持 ：实现 RULES.kdl 解析器
2. 文学素材库 ：创建 literary_materials.tsv
3. 性能监控 ：添加性能指标收集
### 长期规划（3-6 月）
1. 机器学习模型 ：使用 ML 优化路由决策
2. 分布式支持 ：支持远程数据库
3. Web 界面 ：提供可视化操作界面


我想要将这部分要求按照"活数据 > 死配置
行为约束 > 架构描述
单线程闭环 > 分布式幻觉
用户意图锁 > 系统默认值"规范  .单线程现实：Trae 是单线程 Agent，不存在子进程、黑板机制、分布式共识——写这些会产生"伪调度"幻觉
零死代码原则：只写能转化为 Tool Call（read/write/search）或明确状态（halt/abort）的指令，不写系统架构名词（如 cache=static_prefix）
OODA 闭环：plan 层即包含 Check 阶段，drift=abort 放在此处即是最终熔断，无需独立的 Guard 层守护进程
正交性保护：split=dag（任务拓扑）与 search=min（搜索粒度）完全独立，DAG 并行不意味着可以全局盲搜
否定词优化：ban=hardcode_secret 优于 secret=no_log_hardcode，正向动词的注意力权重大于否定词  设计进行kdl文件.效率优先：拒绝冗余对话，追求信息密度极限
格式契约：KDL（Key-Driven Language）是唯一认可的结构化语言
准确性至上：宁可 halt（暂停），也不 guess（猜测）




----


基于论文《面向单线程IDE智能体的极简控制论》，核心要点如下：

**一、架构祛魅：单线程现实（Anti-Hallucination）**
*   **物理存在性法则**：禁止写入当前环境未暴露API的架构名词（如`subagent`、`blackboard`、`consensus`、`独立Guard层`）。
*   **无调度真相**：LLM是自回归Token预测器，单次执行流中不存在进程调度能力，伪并发指令只会产生"假装调度"的幻觉文本，消耗算力且掩盖执行断层。

**二、活代码范式（Live Code Paradigm）**
*   **判定标准**：指令必须能直接映射为一次IDE工具调用（Tool Call）或明确的输出格式约束，否则即为"死代码"。
*   **死代码示例**：`scratch=on`（无API支撑）、`verify=post`（OODA循环已隐含验证，重复声明不产生动作）。
*   **活代码示例**：`write=diff_only`（直接约束文件工具）、`chk src=top3`（直接映射搜索策略）。
*   **行为即指令**：采用"动词+极简参数"的命令式结构，使Prompt退化为面向Agent的极低级汇编语言。

**三、正交控制论：OODA内聚与解耦（Orthogonal Control）**
*   **唯一主轴**：采用OODA（观察-调整-决策-行动）作为唯一控制循环，取代模块化堆砌。
*   **维度正交性**：宏观规划维度（`split=dag`）与微观执行维度（`search=min`）相互独立、无冲突，实现乘法级约束力。
*   **状态跃迁强断言**：
    *   `halt=vague`：歧义即停，强制跃迁至"等待人类输入"状态。
    *   `drift=abort`：在OODA的Check阶段发现偏离即熔断，无需独立守护层，Check即单线程的最高裁决权。

**四、极致Token经济学（Token Economics）**
*   **载体选择**：KDL（`node key=value`）替代JSON/YAML，消除`{}[]"`等包裹符号，实现零噪音解析。
*   **正向约束原则**：避免否定词（`no`、`don't`）注意力衰减，采用正向实词（`ban=secret` > `secret=no_log`）。
*   **输出熵减**：通过`ban=ack`（禁止确认礼仪）与`noise=0`（零冗余）双重锁定，强制100%的Token转化为AST代码或工具参数。

**五、终极范式（Ultimate Form）**
*   **定义**：将Agent降维为一个"基于滑动窗口、受限于正交规则集、仅通过Tool_Call与外界交互的OODA循环器"。
*   **终点**：提示词不是写给人看的文档，而是写给状态机的硬连线配置；任何不能转化为动作的修饰语，均是对模型算力的犯罪。
*   

----

基于论文《面向单线程IDE智能体的极简控制论：从"架构幻觉"到"活指令"的提示词架构设计原则》，核心要点如下：

**一、本质定义：Agent的物理现实**
*   **Willison共识**：Agent = "An LLM agent runs tools in a loop to achieve a goal"（在循环中运行工具以达成目标的LLM）。
*   **单线程约束**：IDE Agent（Cursor/Trae）是**单线程、无状态、自回归**的Token预测器，无进程调度能力，无独立守护线程。

**二、核心误区：架构幻觉（Architectural Hallucination）**
*   **死代码陷阱**：写入当前环境未暴露API的分布式架构名词，导致"伪调度"幻觉与Token浪费。
*   **典型死代码**：`subagent`、`blackboard`、`consensus`、`scratch=on`（无API支撑）、`max_turn`/`max_token`（客户端参数，写入Prompt会导致模型自我截断）。
*   **独立守卫层虚妄**：单线程中无法异步启动Guard线程，熔断必须内聚于控制循环的Check阶段。

**三、活指令范式（Live Code Paradigm）**
*   **唯一判据**：指令必须能直接映射为**Tool Call**、**输出格式约束**或**明确状态跃迁**。
*   **活代码示例**：`write=diff`（直接约束文件工具）、`halt=ambiguous`（歧义即停，状态跃迁）、`ban=hardcode_secret`（AST扫描动作）。
*   **正向约束原则**：避免否定词（`no`/`don't`）注意力衰减，采用"动词+对象"（`ban=secret` > `secret=no_log`）。

**四、控制论闭环：OODA主轴**
*   **替代模块化**：以OODA（Observe-Orient-Decide-Act）或ReAct作为唯一控制主轴，替代"独立守卫层"。
*   **状态跃迁强断言**：
    *   `halt=ambiguous`：歧义时强制跃迁至"等待人类输入"状态。
    *   `drift=abort`：在Orient/Check阶段发现目标偏离，立即跃迁至"终止"状态（单线程的最高裁决权）。
*   **维度正交性**：`split=dag`（宏观任务拓扑）与`search=min`（微观搜索粒度）相互独立，无冲突，实现乘法级约束力。

**五、记忆系统：压缩与索引**
*   **非堆砌聊天记录**：记忆是"可检索的知识+可压缩的历史"。
*   **按需加载**：通过`read=glob_line`按需拉取，避免一次性塞入全文；用"索引+抽象"代替"全文拷贝"。
*   **错误转化为规则**：通过`error=2rule`将历史错误压缩为可执行规则（前置检查），而非保留长文本。

**六、提示词工程：DSL与Token经济学**
*   **载体选择**：采用KDL（`node key=value`）替代JSON/YAML/Markdown，消除`{}[]"`等包裹符号，实现零噪音解析。
*   **操作规格化**：提示词是"接口契约"而非"产品说明书"，每条规则应对应一个可单元测试的工具调用或输出格式。
*   **输出熵减**：通过`ban=ack`（禁止确认话术）与`noise=0`（零冗余）双重锁定，强制100% Token转化为AST代码或工具参数。

**七、安全与权限边界**
*   **读优先，写确认**：读操作可自动执行；写/破坏性操作需显式确认（`confirm=destructive`）。
*   **密钥管理**：敏感信息禁止明文（`secret`通过环境变量引用，而非`no_log`式否定约束）。

**八、终极范式：硬连线配置**
*   **终点定义**：提示词不是"写给人看的文档"，而是"写给状态机的硬连线配置"。
*   **Agent降维**：将Agent视为"基于滑动窗口、受限于正交规则集、仅通过Tool_Call与外界交互的OODA循环器"。
*   **物理存在性**：任何不能转化为动作的修饰语，均是对模型算力的犯罪。
